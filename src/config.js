// src/config.js
const config = {
    API_KEY: 'your_api_key_here', // Replace with your API key
    cities: ['Delhi', 'Mumbai', 'Chennai', 'Bangalore', 'Kolkata', 'Hyderabad'],
    alertThreshold: 35, // Example threshold
    checkInterval: '*/5 * * * *' // Every 5 minutes
};

module.exports = config;
